NODE_ENV=development
PORT=3001
TCP_PORT=4001
REDIS_HOST=localhost
REDIS_PORT=6379
USER_SERVICE_HOST=localhost
USER_SERVICE_TCP_PORT=4002
JWT_ACCESS_SECRET=dev-only-shared-hs256-secret-change-me
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN_SECONDS=604800

# Google reCAPTCHA v2 ("I'm not a robot" checkbox) — verifies register/login.
# Real key pair registered at https://www.google.com/recaptcha/admin,
# paired with the site key in frontend/.env.example. Make sure "localhost"
# is in this key's allowed domains list when testing locally.
RECAPTCHA_SECRET_KEY=6LfiK6MtAAAAAOA2orFwA0XHas6wyTER4fFIIyVA
