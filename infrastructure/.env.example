# Copied to `infrastructure/.env` for local `docker compose up` — demo-only credentials, never used outside a laptop/CI.
MSSQL_SA_PASSWORD=DemoTech!Order2026
MYSQL_ROOT_PASSWORD=DemoTech!Payment2026
MYSQL_DATABASE=payment_service
MONGO_ROOT_USERNAME=demotech
MONGO_ROOT_PASSWORD=DemoTech!Mongo2026

# Google reCAPTCHA v2 secret used by auth-service to verify register/login.
# This is a real key pair registered at https://www.google.com/recaptcha/admin
# for this project (paired with the site key in frontend/.env.example) —
# make sure "localhost" is in that key's allowed domains list for local dev.
RECAPTCHA_SECRET_KEY=6LfiK6MtAAAAAOA2orFwA0XHas6wyTER4fFIIyVA
